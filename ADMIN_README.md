# 超级管理员后台使用指南

## 功能概述

超级管理员后台提供了完整的用户管理和文件管理功能，包括：

### 1. 系统概览
- 总用户数、活跃用户数、管理员数量
- 总文件数、存储空间使用量、回收站文件数
- 实时统计数据展示

### 2. 用户管理
- 用户列表查看（支持分页）
- 搜索功能（用户名、邮箱、昵称）
- 筛选功能（角色、状态）
- 用户操作：
  - 查看详情：显示用户完整信息和文件统计
  - 启用/禁用：切换用户账号状态
  - 设为管理员/取消管理员：修改用户角色
  - 重置密码：为用户设置新密码
  - 删除用户：禁用用户账号

### 3. 文件管理
- 文件列表查看（支持分页）
- 搜索功能（文件名）
- 筛选功能（文件类型）
- 文件操作：
  - 删除文件：将文件移至回收站

## 如何创建管理员账号

### 方法1: 数据库直接修改（推荐用于首个管理员）

\`\`\`sql
-- 连接到 MySQL 数据库
mysql -u root -p

-- 使用数据库
USE go_drive;

-- 将指定用户设置为管理员
UPDATE users SET role = 'admin' WHERE username = 'your_username';

-- 查看结果
SELECT id, username, email, role, is_active FROM users WHERE role = 'admin';
\`\`\`

### 方法2: 通过管理员后台升级

1. 先使用方法1创建第一个管理员账号
2. 登录管理员后台
3. 在用户管理页面，点击"设为管理员"按钮

## 访问管理员后台

1. 访问地址：`http://localhost:5173/admin`
2. 使用管理员账号登录
3. 系统会自动验证管理员权限

## 路由说明

- `/admin` - 管理员后台首页（自动跳转到 dashboard）
- `/admin/dashboard` - 系统概览
- `/admin/users` - 用户管理
- `/admin/files` - 文件管理

## 权限说明

- 所有管理员接口都需要 JWT Token 认证
- 只有 `role = "admin"` 的用户才能访问管理员后台
- 管理员不能对自己的账号进行以下操作：
  - 禁用自己的账号
  - 删除自己的账号
  - 修改自己的角色

## 安全建议

1. **首个管理员创建**: 建议在部署后立即通过数据库创建第一个管理员账号
2. **管理员数量控制**: 建议管理员账号不超过 5 个
3. **密码安全**: 重置密码时建议使用强密码（至少6位，包含字母、数字和特殊字符）
4. **操作确认**: 所有危险操作（删除、禁用）都需要二次确认

## 技术栈

- React 18 + TypeScript
- React Router v6（路由管理）
- Tailwind CSS（样式）
- Lucide React（图标）
- Axios（HTTP 请求）

## 文件结构

\`\`\`
src/
├── layouts/
│   └── AdminLayout.tsx          # 管理员布局组件
├── pages/
│   └── admin/
│       ├── Dashboard.tsx        # 系统概览页面
│       ├── Users.tsx            # 用户管理页面
│       └── Files.tsx            # 文件管理页面
├── components/
│   └── admin/
│       ├── UserDetailModal.tsx  # 用户详情弹窗
│       └── ResetPasswordModal.tsx # 重置密码弹窗
├── services/
│   └── adminService.ts          # 管理员API接口
├── utils/
│   └── adminUtils.ts            # 工具函数
└── routes.tsx                   # 路由配置
\`\`\`

## API 接口

所有接口详情请参考 `api.md` 文档。

## 常见问题

### Q: 如何访问管理员后台？
A: 访问 `/admin` 路径，系统会自动验证管理员权限。

### Q: 忘记管理员密码怎么办？
A: 可以通过数据库直接修改密码，或者使用其他管理员账号重置密码。

### Q: 删除用户后数据会丢失吗？
A: 删除用户实际上是禁用账号，不会真正删除数据。用户的文件仍然保留。

### Q: 如何查看用户的文件？
A: 在用户管理页面点击"查看详情"，可以看到用户的文件统计信息。

## 开发说明

### 添加新的管理员功能

1. 在 `src/services/adminService.ts` 中添加 API 接口
2. 在 `src/pages/admin/` 中创建新页面
3. 在 `src/routes.tsx` 中添加路由
4. 在 `AdminLayout.tsx` 中添加菜单项

### 自定义样式

所有样式使用 Tailwind CSS，可以直接修改组件中的 className。

## 更新日志

### v1.0.0 (2024-04-03)
- 初始版本发布
- 实现系统概览、用户管理、文件管理功能
- 支持用户搜索、筛选、分页
- 支持用户状态管理、角色管理、密码重置
- 支持文件查看和删除
